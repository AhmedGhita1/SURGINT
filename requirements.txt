# the package and every runtime pin it declares, plus the test dependency.
# pyproject.toml is the single source of truth for those pins.
-e .[dev]

# surgint imports neither of these.
# torchvision is pinned to pair with the torch build ci installs from the
# pytorch cpu index. transformers' detection loss reduces num_boxes across
# processes when accelerate is importable.
torchvision==0.19.1
accelerate==1.1.1
