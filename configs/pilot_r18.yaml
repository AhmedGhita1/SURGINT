experiment_name: pilot_r18

checkpoint: PekingU/rtdetr_r18vd_coco_o365

data:
  root: data/surgical-tools-dummy/pilot

train:
  epochs: 20
  batch_size: 8
  learning_rate: 1e-4
  weight_decay: 1e-4
  seed: 0

output_dir: outputs/pilot_r18
